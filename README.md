# PaintScript Engine

copy "Linked.json" into the output folder then rename the copy as "program.json"

For the compiler go here: https://github.com/CocoInk-Ink/PaintScript-Standard

This is the runtime, it's still being worked on with little to no documentation of
whats going on.